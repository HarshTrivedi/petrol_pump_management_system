<%= simple_form_for(@machine) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :tank %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
