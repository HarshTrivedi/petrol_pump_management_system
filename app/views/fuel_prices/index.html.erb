<%- model_class = FuelPrice -%>
<div class="page-header">
  <h1><%=t '.title', :default => FuelPrice %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:type) %></th>
      <th><%= model_class.human_attribute_name(:price) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @fuel_prices.each do |fuel_price| %>
      <tr>
        <td><%= fuel_price.id %></td>
        <td><%= fuel_price.type %></td>
        <td><%= fuel_price.price %></td>
        <td><%=l fuel_price.created_at, format: :long %></td>
        <td>
          <%= link_to fuel_price_path(fuel_price), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to fuel_price_path(fuel_price), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_fuel_price_path,
            :class => 'btn btn-primary' %>
