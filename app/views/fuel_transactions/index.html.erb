<%- model_class = FuelTransaction -%>
<div class="page-header">
  <h1><%=t '.title', :default => FuelTransaction %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:quantity) %></th>
      <th><%= model_class.human_attribute_name(:machine_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @fuel_transactions.each do |fuel_transaction| %>
      <tr>
        <td><%= fuel_transaction.id %></td>
        <td><%= fuel_transaction.quantity %></td>
        <td><%= fuel_transaction.machine_id %></td>
        <td><%=l fuel_transaction.created_at, format: :long %></td>
        <td>
          <%= link_to fuel_transaction_path(fuel_transaction), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to fuel_transaction_path(fuel_transaction), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_fuel_transaction_path,
            :class => 'btn btn-primary' %>
